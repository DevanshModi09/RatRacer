{
  "name": "ratracer",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && tsc -b --prefix backend npm install --prefix frontend && npm run build --prefix frontend ",
    "start": "npm run start --prefix backend"
  }
}